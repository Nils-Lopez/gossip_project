<!DOCTYPE html>
<html>
<head>
	<title>GossipProject - Home</title>
</head>
<body style="margin-left: 10%; margin-right: 10%;">
  <br>
  <br>
  <center>
  <div class="card w-50">
        <div class="card-header">
          <h3 class="card-title"><i class="fas fa-user"></i> <%=User.find(@id).first_name + " " + User.find(@id).last_name%></h3>
        </div>
        <div class="card-body">
 		     
 		     <p class="card-text"><i class="fas fa-calendar-alt"></i> <%=User.find(@id).first_name%> a rejoint Gossip Project le <%=User.find(@id).created_at.strftime("%d/%m/%Y")%></p>
 		     <p class="card-text"><i class="fas fa-birthday-cake"></i> <%=User.find(@id).age%> ans</p>
         <p class="card-text"><a href="mailto:ddd@ddd.com"><i class="fas fa-envelope"></i> <%=User.find(@id).email%></a></p>
         <p class="card-text"><i class="fas fa-city"></i> Originaire de <%= link_to User.find(@id).city.name + " " + User.find(@id).city.zip_code, "../cities/#{User.find(@id).city.id}" %></p>
         <p class="card-text"><i class="fas fa-pen-nib"></i> <%=User.find(@id).desc%></p>
         <h4 class="card-text"><i class="fas fa-chart-bar"></i> Statistiques :</h4>
         <p class="card-text">Nombre de gossips : <%=User.find(@id).gossips.count%></p>
         <p class="card-text">Nombre de likes : <%=User.find(@id).likes.count%></p>
         <p class="card-text">Nombre de commentaires : <%=User.find(@id).comments.count%></p>
 		</div>
 	</div>
  </center>
</body>
</html>